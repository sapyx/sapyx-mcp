{
  "name": "pinterest-mcp",
  "version": "1.0.0",
  "description": "MCP server for Pinterest - catalog and manage pins via Claude",
  "type": "module",
  "bin": {
    "pinterest-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node build/index.js",
    "build:mcpb": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=mcpb-dist/server/index.js --format=esm && mcpb pack mcpb-dist/ pinterest-mcp.mcpb"
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.0",
    "dotenv": "^16.4.0",
    "open": "^10.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
